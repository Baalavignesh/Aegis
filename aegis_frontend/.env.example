# Backend API base URL (used by the frontend to fetch agents, logs, approvals).
# The backend reads from MongoDB; configure MONGO_URI and MONGO_DB_NAME in aegis_backend.
VITE_API_URL=http://localhost:8000
