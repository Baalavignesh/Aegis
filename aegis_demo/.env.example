# ─── Aegis Demo — Environment Variables ──────────────────────────
# Copy this file to .env and fill in the values.
#   cp .env.example .env

# ─── Required ────────────────────────────────────────────────────

# Google Gemini API key (get one at https://aistudio.google.com/apikey)
GOOGLE_API_KEY=

# MongoDB connection string (used by banking tools for direct data queries)
MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?appName=Aegis
MONGO_DB_NAME=sentinel_db

# ─── Optional (defaults shown) ───────────────────────────────────

# Gemini model (gemini-2.5-flash-lite = 1000 RPD free tier)
# GEMINI_MODEL=gemini-2.5-flash-lite

# Backend API URL (SDK sends governance requests here)
# AEGIS_BACKEND_URL=http://localhost:8000

# LangSmith tracing (optional — sign up at https://smith.langchain.com)
# LANGSMITH_API_KEY=
# LANGSMITH_TRACING=false
# LANGSMITH_PROJECT=aegis-demo
